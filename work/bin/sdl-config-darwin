#!/usr/bin/perl

for (@ARGV) {
    if ($_ eq '--libs') {
        print "-framework SDL -framework Cocoa -framework OpenGL ";
    }
    elsif ($_ eq '--cflags') { }
    elsif ($_ eq '--version') {
        print "1.2.8 ";
    }
}

print "\n";
